import React from 'react'
import { ActivityIndicator, View } from 'react-native';

export const LoadingProgress = () => {
    return (
        <View>
            <ActivityIndicator/>
        </View>
    )
}
